# Prod environment overrides
# GitHub Actions updates image.repository and image.tag on every merge to main

image:
  repository: "REPLACE_WITH_PROD_ECR_URL"  # ← prod ECR URL when ready
  tag: "latest"

replicaCount: 3

ingress:
  host: easyshop.yourdomain.com

env:
  NEXT_PUBLIC_API_URL: "https://easyshop.yourdomain.com/api"
  NEXTAUTH_URL: "https://easyshop.yourdomain.com"

mongodb:
  storageSize: 20Gi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 65

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi